import AsyncBtnModal, {
  AsyncBtnModalProps,
  AsyncBtnModalRefType,
} from './AsyncBtnModal';
import SortTableModal, { SortTableModalProps } from './SortTableModal';
import TriggerModal from './TiggerModal';
import type {
  ModalSizesType,
  TiggerModalProps,
  TiggerModalRefType,
} from './config';

export { AsyncBtnModal, SortTableModal, TriggerModal };
export type {
  AsyncBtnModalProps,
  AsyncBtnModalRefType,
  ModalSizesType,
  SortTableModalProps,
  TiggerModalProps,
  TiggerModalRefType,
};
